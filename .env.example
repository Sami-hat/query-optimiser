# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pg_analyser
DB_USER=postgres
DB_PASSWORD=password

# Connection Pool
DB_POOL_MIN=2
DB_POOL_MAX=10

# API Configuration
API_KEYS=                    # Comma-separated API keys (leave empty to disable auth)
CORS_ORIGINS=*               # Comma-separated origins (use specific domains in production)

# Rate Limiting
RATE_LIMIT_REQUESTS=100      # Max requests per window
RATE_LIMIT_WINDOW=3600       # Window in seconds (1 hour)
