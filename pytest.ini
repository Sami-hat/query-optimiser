[pytest]
# Put the repo root on sys.path so `pytest` works the same as `python -m pytest`
# (without this, the tests' `from src...` imports fail on a bare `pytest` call).
pythonpath = .
testpaths = tests
